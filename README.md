Base Django files for any kind of website/app. Has docker and can be run on local machine by "docker-compose build" then "docker-compose up".
Make sure u pip install -r requirements.txt before everything.
